[project]
name = "monitoring-agent"
version = "2026.2-b"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "a2a-sdk<1.0.0",
    "bat-adk==2026.2.26",
    "br-rapp-sdk==2025.12",
    "nvidia-nat==1.4.1",
    "nvidia-nat-a2a==1.4.1",
    "nvidia-nat-langchain==1.4.1",
    "nvidia-nat-mcp==1.4.1",
    "nvidia-nat[phoenix]==1.4.1",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

# [tool.uv.sources]
# br-rapp-sdk = { path = "../../br-rapp-sdk" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
